# Setup Rápido

1. Importe todos os arquivos de `n8n-superpack/workflows/` no n8n.
2. Configure variáveis de ambiente em `Settings > Variables` conforme `variables.md`.
3. (Opcional) Crie tabela de logs no Supabase:

```sql
create table if not exists wbb_logs (
  id bigint generated by default as identity primary key,
  timestamp timestamptz,
  tenant_id text,
  lead_id text,
  evento text,
  status text,
  erro text,
  metadata jsonb
);
```

4. Ative os workflows `[WBB] 01..10`.
5. Execute `[WBB] 00 - Mock Generator` para disparar eventos fake.

## Checklist de validação
- Webhooks `wbb-lead-intake`, `wbb-qualificacao`, `wbb-handoff` respondem `ok=true`.
- Logs inserindo em `wbb_logs` (ou placeholders em mock).
- Notificações chegando no webhook configurado.
- Follow-up respeitando `max_tentativas`.
